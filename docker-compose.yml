version: "3.9"
services:

  corbit-auth-basic:
    build:
      context: ./corbit-auth/
      dockerfile: Dockerfile
    ports:
      - "8080:8080"

  nginx:
    build:
      context: ./nginx/
      dockerfile: Dockerfile
    ports:
      - "80:80"
    depends_on:
      - corbit-auth-basic
