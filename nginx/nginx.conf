events {
    worker_connections 1024;
}

http {
    upstream container-auth {
        server auth-service:8080;
    }

    upstream container-keycloak {
        server keycloak-service:8080;
    }

    upstream container-frontend {
        server frontend:8080;
    }

    server {
        listen 9093;

        location = / {
            proxy_pass http://localhost:9094/auth-page;
        }

        location = /auth {
            auth_request /auth-check;
            error_page 401 = @unauthorized;

            proxy_pass http://container-frontend/;
        }

        location = /auth-check {
            proxy_pass http://localhost:9094/auth;
        }

        location @unauthorized {
            return 401;
        }
    }

    server {
        listen 9095;

        location / {
            proxy_pass http://container-keycloak;
            proxy_set_header Host $host:9095;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Host $host;
            proxy_set_header X-Forwarded-Port $server_port;
            proxy_set_header X-Forwarded-Proto $scheme;
        }
    }

    server {
        listen 9094;

        location / {
            proxy_pass http://container-auth;
            proxy_set_header Host $host:9094;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Host $host;
            proxy_set_header X-Forwarded-Port $server_port;
            proxy_set_header X-Forwarded-Proto $scheme;
        }
    }
}